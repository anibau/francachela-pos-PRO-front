/** Límite por defecto para listados del backend (evita el default de 10 del servidor) */
export const DEFAULT_LIST_LIMIT = 500;

export const DEFAULT_LIST_PARAMS = {
  page: 1,
  limit: DEFAULT_LIST_LIMIT,
};
